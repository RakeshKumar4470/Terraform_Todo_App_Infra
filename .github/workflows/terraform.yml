name: Terraform TODOAPP Infra Pipeline
on: push
jobs:
  terraform:
    name: Terraform Init
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.8.5

      - name: Terraform Init
        run: terraform init
        working-directory: ./\Terraform_Todo_App_Infra\Todo_Infra\Environment\dev

      - name: Terraform Plan
        run: terraform plan



